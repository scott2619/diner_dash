<%= link_to "View Cart.", "#" %>

<% @items.each do |item| %>
  <%= item.title %>
<% end %>

<% @categories.each do |category| %>
  <%= link_to category.name, category_path(category) %>
<% end %>
